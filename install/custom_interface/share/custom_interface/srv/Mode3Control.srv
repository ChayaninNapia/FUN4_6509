# Mode3Control.srv

# Request
float64[] target_joint_positions  # Array of target joint positions

---
# Response
bool success   # Indicates if the mode 3 controller successfully moved to the target joint positions
string message # Optional message providing more detail (success or failure)
